import { DirectivesColorDirective } from './directives-color.directive';

describe('DirectivesColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesColorDirective();
    expect(directive).toBeTruthy();
  });
});
